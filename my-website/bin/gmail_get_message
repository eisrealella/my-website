#!/usr/bin/env bash
set -euo pipefail
source "$HOME/.openclaw/workspace/bin/gmail_tool_common.sh"

if [[ $# -lt 1 ]]; then
  echo "Usage: gmail_get_message <message_id>" >&2
  exit 1
fi

run_gmail_skill get-message --id "$1"
